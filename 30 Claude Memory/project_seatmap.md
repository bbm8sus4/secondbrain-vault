---
name: project_seatmap
description: SeatMap — single-file HTML office seating/org/product app for Thunder+EasySlip. STATUS DONE (2026-07-09, Bob ปิดโปรเจกต์). Handoff doc + methodology if reopened.
metadata: 
  node_type: memory
  type: project
  status: completed
  completedDate: 2026-07-09
  originSessionId: 27a1612c-22b0-4899-8090-0137499b2074
---

> ✅ **โปรเจกต์เสร็จสมบูรณ์ — Bob ปิดงาน 2026-07-09.** ส่งมอบครบ: แอป deploy (Cloudflare Pages, gate), i18n TH/EN ครบทุกจุด (v3.9.0 / checkpoint seatArh_v.1.0.2), public repo https://github.com/bbm8sus4/office-seating-plan. ถ้ามีงานเพิ่มค่อยเปิดใหม่ — รายละเอียด/วิธีทำงานด้านล่างเก็บไว้อ้างอิง.

**SeatMap** = single-file HTML app จัดผังที่นั่ง + ผังองค์กร + product/lane board + HR dashboard + master directory ของ Thunder Solution + Easy Slip (~30 คน). ไม่มี backend, เก็บใน localStorage + IndexedDB.

- **ไฟล์จริง:** `~/Desktop/office-seating-overview.html` (ชื่อในแอป SeatMap, accent ม่วง #7c3aed). เวอร์ชันดูที่ `APP_VERSION` บรรทัดแรก `<script>` (ล่าสุด v3.8.0). **⚠ 2026-07-05: บาง session (ghostty) macOS บล็อกอ่าน/เขียน `~/Desktop` (EPERM/Operation not permitted) — แก้ + deploy จาก `~/Work/office-seating-deploy/dist/index.html` แทน (เป็นตัว deploy จริงอยู่แล้ว) แล้วค่อย sync กลับ Desktop ตอนสิทธิ์ปกติ. ถ้า Desktop เขียนไม่ได้ ให้ถือ dist/index.html เป็น source ชั่วคราว.**
- **Handoff เต็ม (อ่านก่อนทำงานต่อ):** `~/SecondBrain/01 Projects/Office Seating Plan/HANDOFF.md` — มีวิธี verify, architecture, conventions, backlog, open questions ครบ.
- **รายการ fix ละเอียด:** `~/SecondBrain/01 Projects/Office Seating Plan/README.md`

**กติกาเหล็ก:** (1) ห้ามพัง — verify ทุกการแก้; (2) ห้าม deploy/ข้อมูลจริงหลุด cloud (เคยถอน deploy เพราะหลุด — ตอนนี้ gate ด้วยรหัส); (3) single-file constraint; (4) **ห้ามแตะ ONBOARDING TRAY** (`#trayPanel` / `.panel` / `.person-chip` / `.nb` แผงคนรอจัดที่ขวามือในหน้าที่นั่ง) — ผู้ใช้สั่งให้คงดีไซน์เดิม อย่า restyle.

**Shared backend (2026-07-03):** เพิ่ม Cloudflare KV (`SEATMAP_PHOTOS` ns `a5ebe2f0375348619a52e15b406c32bc`) + API `/api/state` ใน `_worker.js` (หลังรหัส) → **shared state ทุกคนที่ได้ลิงก์แก้ร่วมกัน** เห็นชุดเดียว. client: `_initSharedSync/_applyShared/_sharedPushNow/pullShared` (poll 10s + focus), รูป merge ทุกเครื่องไม่หาย, last-write-wins. wrangler.toml + deploy จาก `~/Work/office-seating-deploy/` (มี KV binding). รหัสตอนนี้ = `thunder2026` (password-only, username เว้นว่างได้).

**Verify ด้วย Chrome MCP ผ่าน HTTP** (tools ไม่โผล่ใน tool list): `curl` ไป `http://127.0.0.1:12306/mcp` — initialize เอา session id → `chrome_javascript`/`chrome_click_element`/`chrome_screenshot`/`chrome_console` บน tab ที่เปิดไฟล์ (หา tabId ด้วย `get_windows_and_tabs`). Smoke script: `/tmp/chrome_mcp_final.sh <SID>` (2 fail = artifacts, ไม่ใช่ bug). วิธีเต็มอยู่ใน HANDOFF.md §3.

**Traps:** ห้ามใช้ `window.prompt/confirm/alert` (Chrome block → ใช้ `openInputModal`); ทุก avatar photo ผ่าน `_photoBg()` (XSS); org undo stack per-company; test ด้วยคลิกปุ่มจริง.

**Big feature round (2026-07-03):** (1) คนอยู่ได้หลายโปรเจกต์ — `person.product/lane` → `person.products=[{product,lane}]`, roster ขวาโชว์ทุกคน (persistent), ลากเพิ่มได้เรื่อย ๆ, × เอาออกทีละโปรเจกต์; (2) เพิ่ม field `person.employment` (fulltime/parttime/freelance/intern) + status `remote`; (3) chip โปรเจกต์ลากสลับได้ + ปุ่มเดียว "จัดการโปรเจกต์" (projManagerModal); (4) ผังองค์กร **เพิ่มบริษัทได้** — `ORG_BUILTINS`+dynamic `ORG_DEFS`+`rebuildOrgDefs()`, registry `osp.org.companies.v1`, per-company key `osp.org.user.<id>.v1`; org node มีปุ่ม hover แก้/เพิ่มลูกน้อง (`.oact`); (5) Dashboard หน้าเดียว `.dash-mosaic` 6 card + Export PDF (`exportDashboard`=print); (6) ธีมสว่างเป็น default. ทั้งหมดผ่าน CRUD-basic audit (แก้ 3 critical+6 major: org cross-tab sync, modal z-index stack, org-link undo, iOS zoom, org mobile scroll, chip=pill=donut consistency, confirm ลบโปรเจกต์ ฯลฯ) + verify Chrome MCP.

**Deployed (2026-07-03, v3.8.0):** private + edge-auth gated ที่ https://seatmap-bb85fd28.pages.dev (Cloudflare Pages, user `thunder`). กติกาข้อ 2 (ห้ามข้อมูลจริงหลุด) ยังคงอยู่ — deploy นี้ปลอดภัยเพราะ `_worker.js` gate ที่ edge ทุก request (ไม่ auth = 401 ไฟล์ไม่ถูกส่ง). artifact + คำสั่ง redeploy/rotate/take-down อยู่ที่ `~/Work/office-seating-deploy/` + `SecondBrain/01 Projects/Office Seating Plan/DEPLOY.md`. **ห้าม deploy public แบบไม่มี gate เด็ดขาด** (มี PII จริง 54 ชื่อฝังใน source).

**Roster จริง (โหลดแล้ว 2026-07-03):** DEFAULT_PEOPLE = 47 คน = Thunder 25 + Easy Slip 13 + incoming placeholder 9 (nh-*). แต่ละคนมี: คำนำหน้า (นาย/นางสาว) นำหน้า fullName ไทย · ตำแหน่งภาษาอังกฤษ · พื้นที่ทำงาน WFH→status `remote`, On-site→`active`. Nay (มหาสมุทร) เป็น dual Thunder+EasySlip → set company Thunder (primary). ที่มา: ชีท Google รายชื่อพนักงานจริงที่ผู้ใช้ส่ง. incoming 9 ใบ = คนกำลังจะรับ (เก็บไว้). **เป็นข้อมูล PII จริง — ห้าม deploy public ไม่มี gate.**

**ทิศทาง (2026-07-03):** ผู้ใช้ยืนยันแอปนี้ = **Employee Blueprint** ดูข้อมูลพนักงานเชิงลึก/ข้อมูลส่วนบุคคลละเอียดอ่อน — **ไม่ใช่ระบบ HR เช็คขาด-ลา-มาสาย**. ถอด status `on-leave`/"ลางาน"/"คนลา" + seat dim + dashboard leave metric + i18n `st.onleave`/`hr.onleave`/`hr.ins.onleave` ออกหมดแล้ว (สถานะเหลือ active/new-hire/remote). Manage = ตาราง compact (แถว 44px, header sticky, zebra, scroll ในกล่อง) + toolbar แถวเดียว (search+filter ซ้าย / Add person·room·templates ขวา, สูง 38px) ไม่มีหัวข้อ hero. **Blueprint (2026-07-03, เสร็จแล้ว):** ผู้ใช้เลือก 3 กลุ่ม → เพิ่ม field ใน 
person: phone,email,lineId,birthdate,address,emgName,emgRelation,emgPhone,bloodType,allergies,medical,salary,probationEnd (startDate/employment มีอยู่แล้ว). ข้าม การศึกษา/ทักษะ. UI: modal `#blueprintModal` เปิดด้วย `openBlueprint(id)` (คลิกชื่อ หรือปุ่ม "ดูข้อมูล" ในตาราง Manage) โชว์ 5 section (Contact/Personal/Emergency/Employment/Compensation) + คำนวณ อายุ (`_ageFrom`)/อายุงาน (`_tenureFrom`) + เงินเดือน mask `••••••` มีปุ่ม reveal (`_bpToggleSalary`). ฟอร์มแก้ไข (personModal) เพิ่ม field ครบ อ่านใน `_readPersonForm` + เติมด้วย `_bpFillForm`. migrate default = null. **เงินเดือน/บัตร = PII อ่อนไหวมาก — gate ด้วยรหัสเท่านั้น ห้ามหลุด public.**

**Org Band Manager (2026-07-03):** ย้าย levels จาก constant tuples → `orgState.levels` = `[{key,label,color}]` (init จาก def ผ่าน `_seedLevels`/`_ensureOrgLevels` ใน migrateOrg, clamp node.lv). ทุกบริษัทแก้ได้ (built-in + user). ปุ่ม "จัดการเลน" ใน toolbar org → `openOrgBands()` modal `#orgBandsModal`: เพิ่ม/แก้ชื่อ/สลับ↑↓/ลบ/เปลี่ยนสี (color picker) แต่ละเลน — `orgBand{Add,Rename,Color,Move,Delete}` remap node.lv ให้อัตโนมัติ + pushOrgUndo + saveOrg. renderOrg ใช้ `lvl.color` inline (override CSS เดิม). `_defaultBandColor(company,i)` palette ต่อบริษัท. fillOrgLevelSelect + link modal อ่าน label จาก object. levels sync ผ่าน org store เดิม.

**PO analysis + hardening (2026-07-04):** รัน workflow วิเคราะห์ PO 6 เลนส์ → เอกสาร `~/SecondBrain/01 Projects/Office Seating Plan/SeatMap-PO-Analysis.html` (dark-purple). จุดยืน: internal tool อย่า productize, positioning = "Living Org Map" ไม่ใช่ Employee Blueprint/HRIS. แก้ "Now" 3 อย่างแล้ว: (1) **optimistic lock** — client ส่ง `baseVer:_sharedVer`, worker คืน **409 {conflict,ver,data}** ถ้า stale (ไม่ silent overwrite); client 409 → apply data ล่าสุด + toast เตือน; (2) **schema guard** worker คืน 400 ถ้า `data.state.people` ไม่ใช่ array (กัน poison); (3) **PII gates** — confirm ก่อน Export JSON/exportAll + confirm ก่อนโชว์เงินเดือน. worker มี marker `x-seatmap-wv:2` (ใช้ยืนยัน worker ใหม่ live ก่อน POST-test เพื่อไม่โดน worker เก่าเขียนทับ). **ยังไม่ทำ (รอ Bob):** rotate รหัส thunder2026 → secret (ล็อกเอาต์คนที่รู้รหัสเก่า ต้องแจกใหม่). Next: Cloudflare Access+SSO, field-level PII gating, viewer role, แยกรูปออกจาก state.

**UI polish + product lanes + Manage-as-master (2026-07-05):** (1) หน้าต่าง "เพิ่มบุคคล (ผังองค์กร)" ถอดขีด `—` ในทุก dropdown placeholder + เปลี่ยนคำงง: "ไม่ผูก(โหนดยืนเดี่ยว)"→"ไม่เลือก (กรอกข้อมูลเอง)", "ไม่มี(ไม่ขึ้นกับใคร)"→"อยู่บนสุด (ไม่มีหัวหน้า)", "ขึ้นกับ/Reports to"→"หัวหน้า (รายงานตรงต่อ)", "ตัวสำรอง/Backup"→"ตัวสำรอง (ถ้ามี)", "คอลัมน์(ฝ่าย 1-5)"→"อยู่คอลัมน์ที่ (1-5)", "ความเสี่ยง·บุคคลสำคัญ(key person)"→"ความสำคัญ·เป็นคนสำคัญ (ขาดไม่ได้)". (2) rename ทุก "โปรเจกต์"→"โปรดักส์" ทั้งไฟล์ (display strings ล้วน ปุ่มเป็น "+ โปรดักส์"). (3) **หน้า product มีปุ่ม "จัดการเลน"** เหมือน org — `openProdLanes()` + modal `#prodLanesModal` + `prodLane{Add,Rename,Move,Delete}` (reuse `.obd-*` CSS, no color) จัดเลนของโปรดักส์ที่เลือก (ลบเลน→คนย้ายไปเลนแรก, เลนสุดท้าย→หลุดจากโปรดักส์). ปุ่ม toolbar `data-view="product"`. (4) **Manage = master ของพนักงานทุกคน**: `saveOrgNode()` ถ้าโหนดไม่ได้ผูก personId → soft-match ชื่อกับ state.people ก่อน ถ้าไม่เจอ **สร้าง person ใหม่เข้า state.people** (`_personRecordFromOrg` schema ครบ Blueprint=null) แล้ว set node.personId + save() + toast "เพิ่มเข้าหน้าจัดการแล้ว" → CEO/CTO ที่พิมพ์เองในผังไปโผล่หน้าจัดการด้วย. deploy + verify Chrome MCP ครบ (functions/modal/render/record shape) ข้อมูลจริงไม่แตะ (47 คน/5 products ตลอด). syntax-check 2 script blocks ผ่าน.

**Toolbar unified ทุกหน้า (2026-07-05):** ผู้ใช้สั่ง "ยึดหน้า org เป็นหลัก" — เดิม toolbar แต่ละหน้าไม่เท่ากัน (ที่นั่ง=การ์ดขอบมนลอย, org=แถบเต็มกว้างมีเส้นใต้, จัดการ=แถวลอยเปล่า). แก้ให้ทั้ง 3 หน้าเป็นแถบ sticky เต็มความกว้าง + inner จัดกึ่งกลาง 1480/22px เหมือน org (`.toolbar`+`.tbin`). วิธี: (1) seating ย้าย `.filterbar` ออกจาก `.wrap` เป็นลูกตรงของ `#seatingView` + restyle `.filterbar`=full-width sticky bar (top:43px,border-bottom), `.fb-inner`=max-width:1480/margin auto/padding 10px 22px. (2) manage `#settingsView` padding→0, `.settings-toolbar`=full-width sticky bar, ห่อลูกใน `.settings-toolbar-inner` (1480/22) + ห่อ body (stats/table/hint) ใน `.settings-body` (1480/22). verify Chrome MCP: ทั้ง 3 หน้า bar l=0..vw เต็มกว้าง, inner l/r/w เท่ากันเป๊ะ (412/1892/1480) top=64. ไม่มี error, ข้อมูล 47 คนครบ.

**PO/UX round 2 (2026-07-05):** (1) แก้ hint กล่องจัดการโปรดักส์ (`.pm-hint`) ให้กระชับ ไม่ซ้ำ: "ลากเพื่อจัดลำดับ หรือกดปุ่ม ↑ ↓ · คลิกชื่อเพื่อเปลี่ยนชื่อ · ถังขยะเพื่อลบ". (2) **ยุบคอลัมน์ตารางจัดการ 8→6**: รวม ห้อง+โปรดักส์+ผังองค์กร เป็นคอลัมน์เดียว "ที่นั่ง & สังกัด" (ที่นั่ง=บรรทัดหลัก, โปรดักส์/ผังองค์กร=แท็กเล็กด้านล่าง `.p-tags`, ตัด noise "ยังไม่ผูก" ออก โชว์เฉพาะที่ผูกจริง) + ย่อปุ่ม action (ดูข้อมูล/แก้ไข=text, ผูกผังองค์กร/ยกเลิกที่นั่ง=icon `.p-act-ic`). `placementLabel()` ใน renderSettings, head array เหลือ 6 key, colspan→6, min-width 1120→960. (3) **ลากสลับลำดับคนในเลนโปรดักส์ได้อิสระ**: เพิ่ม `a.ord` ต่อ assignment, renderProducts sort ตาม ord (fallback 1e9=ลำดับเดิม), `updateDrop` คำนวณ `beforePersonId` จากตำแหน่ง pointer เทียบ `.prod-card` + ไฮไลต์ `.drop-before` (เส้นซ้าย), `performDrop` product branch แทรกตำแหน่ง+renumber ord ทั้งเลน + no-op detection (ลากลงที่เดิมไม่ churn). verify Chrome MCP: reorder จริง (reverse→restore ตรง), ตาราง 6 คอลัมน์, ไม่มี error, 47 คนครบ (test แบบ in-memory ไม่ save จริง).

**Room-focus + 7-role audit (2026-07-05):** ผู้ใช้สั่งเน้น "ใครอยู่ห้องไหน" ไม่ใช่ที่นั่ง → เปลี่ยนภาษา: ตัวกรอง "ที่นั่ง"→"การจัดห้อง" (seated/awaiting→"เข้าห้องแล้ว/ยังไม่เข้าห้อง" ผ่าน key ใหม่ lens.place.in/out ไม่แตะ hr.seated/hr.awaiting ที่ dashboard ใช้), view __await→"ยังไม่เข้าห้อง", คอลัมน์ manage "ที่นั่ง & สังกัด"→"ห้อง & สังกัด" (ค่าคือชื่อห้องอยู่แล้ว), empty→"ยังไม่เข้าห้อง". จากนั้น audit 7 role (PO/UXUI/HR/Dev/CEO/COO/CTO) ผ่าน Chrome MCP read-only. **แก้ 3 bug จริง:** (1) [UX] dark-mode: `.toolbar/.filterbar/.settings-toolbar` bg hardcode ขาว rgba(246,247,248,.9) ไม่มี dark override → แถบขาวบนพื้นดำ → เพิ่ม `[data-theme=dark]` bg rgba(18,22,28,.86); (2)+(3) [PO/UX] product ที่มี 0 lane = ทางตัน ลากคนเข้าไม่ได้ → addProduct seed lane "ทั่วไป", performDrop auto-สร้างเลนถ้า product ไม่มีเลน. **ตามด้วย code follow-ups (เสร็จ, v3.9.0):** ปุ่ม "ผูกอัตโนมัติ" (`orgAutoLink()`) ใน toolbar org — จับคู่โหนดที่ยังไม่ผูกกับ person ตามชื่อ (stripped title, unambiguous single match, confirm+undo) · เทสต์ Thunder: ผูก 16/23 โหนดถูกต้อง (stub saveOrg+confirm, restore) · bump APP_VERSION 3.8.0→3.9.0 + changelog. **ข้อมูล (ไม่ใช่ bug — Bob/ทีมต้องกรอก):** Blueprint ว่างหมด (phone/email/emergency/salary/birthdate/startDate = 0/47), org 30 nodes แต่ 0 ผูก person จริง (org กับ directory แยกกัน — ควร bulk-link by name), ห้อง "207 UNKNOWN" ไม่มีชื่อ, ทุกคน employment=fulltime. sharedVer ~143 (มีคนใช้จริง). Test discipline: stub window.save ตอน sim drop + restore state จาก snapshot → KV จริงไม่ถูกเขียน.

**Tray Compact + org card polish (2026-07-05):** (1) **ถาดรอจัดที่ทำใหม่เป็น Compact** (ผู้ใช้เลือกจาก 3 แบบ): `renderUnassigned` จัดกลุ่มตามบริษัท (Thunder/Easy/Other) + ชิปกะทัดรัด `.tray-chip` (avatar+ชื่อ) wrap หลายคอลัมน์ · **คลิกชิป = จัดลงห้อง** → modal `#assignRoomModal` เลือกห้อง (โชว์ "ว่าง N") → ลงที่นั่งว่างแรกของห้องนั้น (`openAssignRoom/_renderAssignRoom/assignToRoomSeat/_freeSeatsInRoom`) · ลากยังได้ (เพิ่ม `.tray-chip` ใน onPointerDown/applySearchFilter, onPointerUp คลิก tray-chip→openAssignRoom แทน editPerson). **(กติกา ONBOARDING TRAY เดิม "ห้ามแตะ" ถูก override ไปแล้วรอบก่อน — ผู้ใช้สั่งทำใหม่เอง)** (2) **ผังองค์กร card** [Image#87/#88]: ถอด badge ตัวย่อบริษัท (`.oco` T/E) บน avatar ออก · ถอดวงแหวนเขียว `.obox.o-linked` (สีเขียวค้างหลัง auto-link — ไม่ใส่ class o-linked แล้ว) + ถอด badge 🔗 · **เพิ่มบรรทัดสถานะใต้ชื่อ**: `_pr.status==="remote"`→"Work from home" (จุดส้ม), มีที่นั่ง→"ที่ออฟฟิศ · [ชื่อห้อง]" (จุดเขียว, โชว์ห้องไม่ใช่ที่นั่ง), ไม่มีที่นั่ง→"ที่ออฟฟิศ · ยังไม่เข้าห้อง" — ดึงจาก `n._personRef` (เฉพาะ node ที่ผูก person). verify: tray 2 กลุ่ม/10 ชิป/คลิก→room modal 5 ห้อง, org oco=0/green=0/🔗=0/status 16 (wfh2 office14).

**Roster + label round (2026-07-05):** (1) ปุ่มมุมมอง (`viewsBtnLbl`) เป็น "มุมมอง" เสมอ ไม่เปลี่ยนตาม view ที่เลือก (i18n `views.btn`, renderLenses set fixed). (2) company lens chips ตัด prefix "T·/E·/O·" ออก → เหลือ "Thunder/Easy/Other" (`segBtn("company",c,c.split(" ")[0])`). (3) **Sidebar หน้าที่นั่งทำใหม่เป็นสไตล์ roster หน้าโปรดักส์** — `renderUnassigned` โชว์ **ทุกคน** (ไม่ใช่แค่รอจัด) + ช่องค้นหา `#traySearch`/`_onTraySearch` (filter in-place ไม่ re-render กัน focus หลุด) + ชิป `.tray-chip`, unseated ก่อน seated (seated จาง `.is-seated` opacity .42), title→"รายชื่อทั้งหมด" (panel.onboarding), poolMini=total. คลิก=จัดลงห้อง, ลาก=ที่นั่ง (เหมือนเดิม). (4) คอลัมน์ manage "สถานะ"→"สัญญาจ้าง" (set.col.status) โชว์เฉพาะ empBadge (ประเภทจ้าง) ตัด stBadge (active/remote) ออก. verify: chips ไม่มีตัวย่อ, roster 47 คน/search "ann"→ANN/seated dim 37, manage header=สัญญาจ้าง cell=ประจำ.

**Org polish + picker + roster scroll (2026-07-05):** (A) org card status ทำให้กระชับเป็น pill เล็ก: WFH→pill teal "WFH" (แมตช์ `.seat .wfh-chip`), ที่ออฟฟิศ→pill โชว์ชื่อห้องเฉย ๆ (ตัด "ที่ออฟฟิศ ·"), `.obox .ostat.owfh/.ooffice/.ono`. (B) **แปลง `<select id=o_personId>` เป็น picker สวย** เหมือน openProdAssign: hidden input + `.picker-btn` (โชว์ชื่อที่เลือก) → คลิกเปิด modal `#orgPersonModal` (search `#opSearch`/`_opFilter` + rows `.person-chip` dot+ชื่อ+ตำแหน่ง+บริษัท+NEW+"(ผูกโหนดอื่นอยู่)") · `openOrgPersonPicker/pickOrgPerson/_setOrgPerson`, `fillOrgPersonSelect`→`_setOrgPerson`. orgPersonPick/saveOrgNode อ่าน hidden .value เหมือนเดิม. (C) ไอคอนปุ่มมุมมอง folder→eye (circle+path). (D) กล่อง roster หน้าที่นั่งล้น → `.tray-panel{display:flex;flex-direction:column;max-height:calc(100dvh-110px)}` + `.pool{flex:1;overflow-y:auto;min-height:0}` (เดิม pool ไม่ scroll เพราะ panel ไม่ flex) → scroll ในกล่อง. verify: eye icon, tray panel 662px pool scroll, org wfh2/office14 pill "WFH"/"209 JETDER", picker 48 rows คลิก ANN→ฟอร์มเติมชื่อ.

**Roster cap + ถอด auto-link (2026-07-05):** (1) roster หน้าที่นั่งยังล้น (max-height:100dvh สูงเกิน ใส่ 47 คนได้หมดไม่ scroll) → `syncTrayHeight()` set inline `maxHeight = min(mainRoomsHeight, vpCap)` ให้กล่องสูงเท่าโซนห้องพอดี scroll ข้างใน (มือถือ ≤860px เคลียร์ maxHeight ปล่อยธรรมชาติ). verify: main 528=tray 528, pool scroll (466<657). (2) **ถอดฟีเจอร์ "ผูกอัตโนมัติ" ออกทั้งหมดตามที่ผู้ใช้สั่ง** — ลบปุ่ม toolbar + function `orgAutoLink` (0 refs). **หมายเหตุ: link ที่ผูกไปแล้ว (personId บน node) ยังอยู่** — ไม่ได้ unlink (เพราะ status WFH/ห้องบน org card พึ่ง link นั้น). ถ้าผู้ใช้อยาก unlink ทั้งหมดต้องสั่งเพิ่ม.

**WFH zone (2026-07-05):** เพิ่มโซน "ทำงานออนไลน์ (WFH)" บนผังที่นั่ง — `renderWfhZone()` box ท้าย `.floor` (ใน #plan/.main) โชว์คน `status==="remote" && !seat` เป็น chip `.wfh-person` (avatar+ชื่อ, ลากได้). เป็น drop target: `updateDrop` เจอ `#wfhZone`→`{type:"wfh"}`, `performDrop` wfh branch → `status="remote"; seat=null`. + ปุ่ม "🌐 ทำงานออนไลน์ (WFH)" ใน room-picker (`_renderAssignRoom` + `setWfhFromPicker`) = คลิกคนในรายชื่อ→เลือก WFH. onPointerDown selector +`.wfh-person`. คน remote+seated (NAY/TOR) ยังอยู่ห้องเดิม (badge WFH) ไม่ซ้ำในโซน. **กล่องรายชื่อยาวเท่ากันอัตโนมัติ** — โซน WFH เพิ่มความสูง .main → syncTrayHeight cap roster ให้เท่า (verify main 708=tray 708). CSS `.wfh-zone/.wfh-person/.ar-wfh` teal. verify: zone 9 คน, drop BOB→remote+noseat (stub save+restore), picker button ✓.

**Filter logic audit (2026-07-05):** ผู้ใช้ทักตัวกรอง "การจัดห้อง" เพี้ยน — audit เจอ inconsistency: placement "awaiting" (=!seat) นับ WFH เป็น "ยังไม่จัด" แต่ roster/dashboard/WFH-zone ถือ WFH=จัดแล้ว. แก้เป็น **single source of truth `_placeState(p)`** = 3 สถานะ exclusive: `seat→"seated"`, `remote&&!seat→"wfh"`, else `"awaiting"`. ใช้ร่วมทั้ง personFilteredOut, lensCount, roster isPlaced, (computeHR awaiting เดิม=!seat&&!remote ก็ตรงอยู่แล้ว). ตัวกรอง "การจัดที่" เป็น 3 ตัวเลือก: อยู่ในห้อง/ทำงานออนไลน์/ยังไม่จัดที่ (lens.place.in/wfh/out). verify: seated29+wfh10+awaiting10=49 total, wfh=WFH zone, seated/awaiting=dashboard เป๊ะ. หมายเหตุ: status "remote" (ทั้งหมด รวม seated เช่น NAY/TOR) ≠ placement "wfh" (remote&&!seat) — ต่างกันโดยตั้งใจ (status axis vs placement axis).

**Hybrid status + WFH reorder (2026-07-05):** (A) เพิ่มสถานะ **hybrid** (ไฮบริด/Hybrid) เป็น status ตัวที่ 4 — i18n st.hybrid, statusLens 4 ชิป (active/newhire/hybrid/remote), f_status + settingsStatusFilter option, migrate valid list +hybrid, stBadge, seat map badge "HYBRID" (สีม่วง `.hybrid-chip` แยกจาก WFH cyan), CSS badge.st-hybrid (settings/bp). hybrid ไม่เข้า WFH zone (zone=remote&&!seat เท่านั้น), placement ตามที่นั่ง. (B) **กล่อง WFH ลากสลับลำดับได้** — เพิ่ม `wfhOrd` sort ใน renderWfhZone, updateDrop คำนวณ beforePersonId (row แนวนอน ใช้ x) + `.drop-before` indicator, performDrop wfh branch reorder + renumber wfhOrd + no-op detection (mirror product lane reorder). verify: statusChips 4 (ไฮบริด 0), reorder KORN/PEAR→PEAR/KORN ได้ (stub save+restore).

**Owner status + form polish (2026-07-05):** (A) form select: ลบขีด "—" (blood type/prod.none→"ไม่ระบุ"/"None"), เพิ่ม custom chevron arrow ให้ `.field select` (appearance:none + bg svg right 11px) สมดุล. (B) audit filter — เพี้ยนเพราะ owner. (C) **เพิ่มสถานะ "owner" (เจ้าของ)** — status ตัวที่ 5, badge OWNER สีทอง, f_status option, migrate valid, stBadge. **owner = ไม่นับเป็นพนักงาน**: `lensCount` return false, `personFilteredOut` owner→filtered out เมื่อมี filter active (show เมื่อไม่กรอง), `computeHR` P=non-owner, `settingsStats` "N พนักงาน"+"N Owner" pill (นับ non-owner), renderSettings manage filter ตัด owner เมื่อ filter/search active, "แสดง X/allPeople". **ตั้ง WOOT(CEO)+PHUM(CTO) = owner แล้ว (save จริง — user request)**. verify live: Thunder lens 27→25, remote 13→11, พนักงาน 47+2 Owner, กรอง Thunder ไม่โชว์ owner.

**Topbar align + PDF export + Blueprint (2026-07-05):** (A) topbar undo/redo (`.topbar-history`) ชิดขวาติดกลุ่มปุ่มขวาทุกหน้า — เปลี่ยน `.topbar-inner{space-between→flex-start}`, `.tabs{flex:1→0 1 auto}`, `.topbar-history{margin-left:auto}` (auto เดียว ไม่งั้น free space แตก 2 ช่อง), `.topbar-status{display:flex→contents}` (wrapper ว่างไม่สร้าง gap → history-utils เหลือ 20px). left=brand+tabs+search, right=history+utils. (B) **Export PDF ทุกห้อง** — `_canvasesToPdf(canvases)` สร้าง PDF เอง (1 ห้อง/หน้า, JPEG ผ่าน DCTDecode, ไม่ใช้ lib) + `exportAllPDF` + menu item (verify blob 70KB application/pdf valid). (C) Blueprint header: "แก้ไข · Edit"→`data-i18n=common.edit` (คำเดียว), ย้าย ✕ เข้ากลุ่ม `.bp-actions` กับปุ่มแก้ไข (bp-x เลิก absolute → flex, bp-head padding-right 34→0) สมดุล.

**i18n + QA รอบผู้ใช้จริง (2026-07-06):** (1) i18n สแกน EN → แก้ hardcoded ไทยทุกจุด (overflow menu/manage select/stats/roster/wfh/product/org buttons/built-in views/dirty badge ผ่าน data-i18n + t()); applyLang เพิ่ม `updateOrgCompanyUI()`. เหลือแค่ "ไทย" (ปุ่มภาษา ตั้งใจ). (2) **เซฟเวอร์ชัน `seatArh_v.1.0.0`** (ก่อน QA) + `v1.0.1` (หลังแก้) ที่ `~/Work/office-seating-deploy/versions/`. (3) **QA แยก 2 sub-agent (UX + engineer) วิจารณ์ตรง ๆ** → แก้ 8 bug จริง: blueprint badge hybrid/owner (เคย fallback Active), owner→WFH กันเสียสถานะ, companyShort i18n, seat "จอง" i18n, Lock label หาย/สลับภาษา, migrateOrg clamp col 1-5, **sync race** (pullShared bail เพิ่ม `_sharedPushT` + _applyShared เคลียร์ timer กัน local edit หาย), empty-states + modal buttons i18n (common.save/cancel/delete). **ยังเหลือ i18n เชิงลึก** (form labels/Blueprint rows/Prefs/confirm×26/alert×14/toast — ~150 strings) — บันทึกใน `SecondBrain/01 Projects/Office Seating Plan/2026-07-06 QA*.md`. feedback ที่ไม่แก้เพราะ Bob สั่งเอง: dashboard ซ่อน, status column→สัญญาจ้าง, ลบปุ่ม unseat, owner หลุด filter.

**i18n เชิงลึกครบ (2026-07-06, v1.0.2):** ทำต่อจนจบ (Bob สั่ง autonomous "ทุกจุด") — (1) **form labels** person/room/org/linkOrg → `data-i18n` (18 + 2 ตัวมี `*` ครอบ `<span data-i18n>` คง `*`); (2) **modal titles** dynamic → `t("mt.*")` 6 จุด; (3) **หน้า Prefs ทั้งหน้า** → `pf.*` (~30) + `renderPrefs` (ปุ่มล็อก/quota hint) ผ่าน `t()`/LANG check, `applyLang` re-render Prefs ถ้า active; (4) **runtime messages** confirm(22)+alert(13)+toast(79) = **163 call site (143 literal ไม่ซ้ำ)** → helper **`L(th,en)`** (วางถัดจาก `t()`, คืน EN ถ้า LANG==="en") — ใช้ Python transformer แทนเฉพาะ string literal ใน argument ของ toast/confirm/alert (ไม่แตะ I18N dict), เลี่ยง double-wrap ด้วยการ process span ขวา→ซ้าย. I18N.en = **254 keys**. verify Chrome MCP (EN mode): เหลือแค่ปุ่ม "ไทย" (ตั้งใจ), Prefs="Settings"/lock="Lock", modal EN ครบ, 49 คน/5 ห้อง/2 owner ไม่แตะ. snapshot `versions/seatArh_v.1.0.2_*`. **Blueprint rows เป็น label สองภาษาในตัว ("ที่อยู่ · Address") ไม่แตะ (ตั้งใจ — EN เห็นครบ)**. APP_VERSION ยัง 3.9.0 (ไม่ bump ตามที่ไม่ได้สั่ง).

**Public GitHub repo (2026-07-06):** https://github.com/bbm8sus4/office-seating-plan (public, remote ชื่อ `smorigin` ใน `~/Work/office-seating-deploy`). ⚠️ **โฟลเดอร์นี้เดิมไม่ใช่ repo ตัวเอง** — git root คือ home (`/Users/aexgee` = repo `ollama-api-chat`) เลย `git init` แยกใหม่. **Bob ตัดสินใจ public ทั้งชื่อจริง 47 คน** (รับความเสี่ยง PII เอง หลังเตือน git history ลบไม่ออก; เงินเดือน/สุขภาพ = null ไม่หลุด). **กฎ push ครั้งหน้า: `.gitignore` ต้องกัน `_worker.js` + `*_worker.js` + `wrangler.toml` + `.wrangler/` (รหัส gate `thunder2026` อยู่ใน worker) — verify `git diff --cached | grep thunder2026` = 0 ก่อน push ทุกครั้ง**. deploy จริง (Cloudflare) ยัง gate ด้วยรหัสเหมือนเดิม worker ไม่ขึ้น public.

ดู [[feedback_secondbrain_vs_work_folder]] — แอปนี้อยู่ Desktop (ยกเว้น convention).
