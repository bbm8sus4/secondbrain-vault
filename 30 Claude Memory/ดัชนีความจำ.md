# Project Memory

- [[โปรไฟล์ - ผู้ใช้ (COO)|User Role — COO]] — COO, MacBook Pro M5 Pro 24GB, Max 20x, prefers decisive exec-level recommendations
- [[โปรไฟล์ - สาย Security|User — security/hacking learner]] — Ongoing study track: ethical hacking, pentesting, API reverse engineering, CTF. Accumulate skills over time.
- [[กติกา - โหมดแฮกเกอร์|Vibe code hacker mode]] — Aggressive never-give-up problem-solving, attacker mindset, vibe-code speed. Within ethical scope only.
- [[บันทึก Session|Session log (auto)]] — One-liner per session appended by SessionEnd hook. Grep for recurring topics and skill progression.
- [[CTF - Bandit บันทึกการเล่น|CTF — Bandit writeup]] — OverTheWire Bandit running log. Currently at Level 5. Resume here next CTF session.
- [[Security Audit 2026-04-29|Security audit 2026-04-29]] — 5 CRITICAL findings across Friday AI / Invoice Hub / OCR Bot. Friday: webhook secret + del IDOR. Hub: plaintext OAuth/IMAP, unsigned state, JWT-in-querystring. **Fix before next prod deploy.**
- [[QA - EasyExchange|QA — EasyExchange (ezxchange.trippyn.cc)]] — 2026-05-31 test, 4 critical (dummy Turnstile, no max amount, no security headers, client-only validation). Bilingual Apple report at /tmp/ezx-report/EasyExchange-QA-Report.html. NOT prod-ready.
- [[กติกา - น้ำเสียงสุภาพ|Tone — keep it respectful]] — Always polite (ครับ/คุณ), never mirror harsh language back. User is the boss.
- [[กติกา - Deploy ใช้ stable URL|Deploy — use stable URL, no double-deploy]] — Cloudflare Pages + GitHub: `git push` only, share stable alias once, never per-commit hash URLs.
- [[กติกา - คุณภาพโค้ด|Code quality — validate before deploy]] — Extract inline client JS from HTML and `node -c` it before deploy; avoid regex with `</` in template literals.
- [[คู่มือ - Chrome MCP กับ Google Docs|Chrome MCP × Google Docs playbook]] — what works (type, F&R via JS, Version History), what doesn't (Cmd+A, Backspace, clipboard), and the safe wholesale-replace workflow.
- [[กติกา - Chrome MCP transport|Chrome MCP transport]] — Use `type: "http"` config. If tools don't load, direct HTTP curl fallback bypasses Claude Code's MCP client entirely. **Multi-profile: each Chrome account on its own port (12306/12307…) + one mcpServers entry per port → run several profiles at once.** Updated 2026-06-04.
- [[กติกา - ลิงก์ Canva ใช้ไม่ได้|Canva MCP links 403]] — Generated `/d/...` URLs always 403. Tell user to find design on canva.com by title instead.
- [[กติกา - ไม่ใช้ AI ในเครื่อง|No local AI in workflows]] — User rejects Ollama/Gemma/on-device AI. Cloud subs only (Claude Max, ChatGPT Plus, Gemini, neomake). 2026-05-01. **Exception:** Typhoon (local Ollama `scb10x/typhoon2.1-gemma3-12b`) IS used on request for Thai proofreading.
- [[คู่มือ - AI Debate|AI debate harness (`aidebate`)]] — `~/ai-debate/debate.py`: Codex+Gemini+Claude propose→anonymized critique→synthesize. Retry+drop-on-fail, history in debates/, `--typhoon` opt-in, `--judge`, `-f`. Rewritten 2026-06-11. Codex auth expiry = known failure (`codex login`).
- [[กติกา - ส่งข้อความหมู่ต้องช้า|Broadcast pacing — slow + careful]] — Mass-send loops (LINE OA / batch DMs): 2x test timings, modal cleanup per iteration, 3–5s+ throttle, snapshot to disk, scan for already-sent before resume.
- [[กติกา - คำสั่งเปิดโปรเจกต์|"เปิด <project>" protocol]] — Direct path → just open. Named alias → find + confirm + remember. Not found → ask before clone, NEVER auto-clone.
- [[กติกา - EasyCRM ลง Obsidian|EasyCRM → save to Obsidian, not memory]] — ทุกข้อมูล EasyCRM ไปลง `~/SecondBrain/EasyCRM/` (Obsidian) เท่านั้น. Memory เก็บแค่ pointer.
- [[กติกา - EasyBOT ลง Obsidian|EasyBOT → save to Obsidian, not memory]] — ทุกข้อมูล EasyBOT ไปลง `~/SecondBrain/EasyBOT/` (Obsidian) เท่านั้น. SessionEnd hook auto-append แชท. **EasyBOT ≠ EasySlip**.
- [[กติกา - BoostSMS ลง Obsidian|BoostSMS → save to Obsidian, not memory]] — ทุกข้อมูล BoostSMS ไปลง `~/SecondBrain/BoostSMS/` (Obsidian) เท่านั้น. Memory เก็บแค่ pointer. อยู่ใต้ EasySlip brand (ไม่ใช่ Thunder).

- [[โปรเจกต์ - Invoice Hub (เลิกแล้ว)|Invoice Control Hub]] — **DEPRECATED 2026-04-29** — User abandoned the project. Do not suggest deploying or working on it.
- [[โปรเจกต์ - Desktop Commander|Desktop Commander config]] — Hardened on 2026-04-08 (narrow allowed folders, zsh, telemetry off). Backup at config.json.backup for rollback.
- [[โปรเจกต์ - OCR Bot|Telegram OCR Bot]] — Thai/English OCR bot on Google Cloud Run (asia-southeast1, service `ocr-bot-api`). Inline FastAPI+Gemini, no Celery. `git push` alone does NOT deploy.
- [[โปรเจกต์ - สรุปประชุมอัตโนมัติ|Meeting Notes Auto-Summary]] — Claude Code Routine สรุป meeting notes อัตโนมัติทุก 1 ชม. repo `bbm8sus4/meeting-notes`, trigger `trig_01MKRtxLwb7treg65GaK8kRE`
- [[โปรเจกต์ - AI Orchestrator|AI Orchestrator (/ai-loop)]] — One-click multi-AI pipeline. `/ai-loop` → loops jobs across Claude Max + ChatGPT Plus (codex) + neomake.io + any web AI (Chrome MCP). Resumable. 2026-04-30.
- [[คู่มือ - Graphify|Graphify skill (/graphify)]] — Knowledge-graph skill installed 2026-05-03. `pipx` + `graphifyy` v0.6.8. Trigger `/graphify .` to map a repo before deep work. Privacy: gate on sensitive repos.
- [[คู่มือ - LINE OA Broadcast|LINE OA broadcast skill (/lineoa-chat-mcp)]] — Per-chat bulk send on chat.line.biz via Chrome MCP. Built 2026-05-06 from EasyBot Affiliate broadcast (250/1245 sent before user stopped). Trigger `/lineoa-chat-mcp`.
- [[คู่มือ - maw|maw setup (multi-agent tmux orchestrator)]] — v26.5.21 installed 2026-06-12, full-power config, oracle convention + traps (dots in --task, --continue kills fresh oracle, snapshot poisoning).
- [[คู่มือ - Cmux|Cmux (multi-agent terminal)]] — macOS app (libghostty/Swift), brew-installed 2026-05-15. Vertical-tab UI for running Claude Code + Codex + other agents in parallel splits, with notifications. `cmux claude-teams` for teammate mode.
- [[คู่มือ - ปลดล็อก Gemini ใน Chrome|Gemini in Chrome unlock (TH account)]] — Patch `Local State` (variations_country→us, is_glic_eligible→true) + `--variations-override-country=us` to surface "✦ Ask Gemini" button on Thai Google accounts. Verified 2026-05-13. PDF + backup saved.
- [[คู่มือ - GitNexus|GitNexus (codebase knowledge-graph MCP)]] — v1.6.4, 16 MCP tools, 7 skills, hooks. Indexed my-ai-bot first. Complements graphify with MCP integration + web UI.
- [[โปรเจกต์ - EasyBOT Finance|EasyBOT Finance (easybot.finance)]] — User's own AI product/company (Gen Picture/BOT API/Graphic) + finance webapp. Path: `~/Desktop/Business/EasyBOT` (07_Development/finance-webapp). NOT EasySlip Bot. 2025 net −฿3.1M. Long-format DB delivered to Drive 2026-05-29.
- [[คู่มือ - อัพโหลด Google Drive MCP|Google Drive MCP upload]] — create_file needs `title`/`contentMimeType`/`textContent` (not name/mime_type). text/csv auto-converts to native Google Sheet. No Sheets API MCP (Drive only).
- [[โปรเจกต์ - Thunder Solution|Thunder Solution / EasySlip business]] — User is COO. 2 brands · slip verification + LINE chatbot · annual target ฿125M · pre-paid cycle = early-month margin always negative (normal). Master Dashboard at master-dashboard-eight-nu.vercel.app.
- [[คู่มือ - ดึงข้อมูล Master Dashboard|Master Dashboard extraction (Chrome MCP)]] — How to pull monthly revenue from master-dashboard-eight-nu.vercel.app: date picker via "เดือนที่แล้ว" preset + real-click, /thunder & /easyslip respect picker (/ does not), BoostSMS under its own tab, chrome_javascript needs `return`.
- [[คู่มือ - โหลดรูป Telegram|Telegram protected photo download]] — Skill `/telegram-download-pic-block`: blob-fetch → new tab → canvas → anchor.click. ~9s/photo. chrome_handle_download unreliable, base64/hex redacted.
- [[โปรเจกต์ - คู่มือ ChatGPT|ChatGPT onboarding guide]] — Thai visual signup guide for trainees, live at chatgpt-setup.pages.dev (CF Pages). Edit via `~/chatgpt-onboarding-guide/build_visual_guide.py` then redeploy. Embedded Thai webfont + PII-redacted screenshots.
- [[คู่มือ - Chrome MCP กับ Google Sheets|Chrome MCP × Google Sheets]] — สร้าง/วางข้อมูลในชีตจริง: pbcopy + osascript Cmd+V (real keystroke) เวิร์ก · CDP paste/execCommand ไม่เวิร์ก · อย่า rename ผ่าน Cmd+A/V (ทับ grid!).
- [[โปรเจกต์ - Dashboard รายจ่าย|Expense Dashboard (Thunder/EasySlip)]] — `~/Desktop/รายจ่าย Thunder&Easyslip.html` single-file dashboard, iterated via "Aidebate <topic>". No emoji, light default, vanilla JS. Design system + human+AI principles + AI features (forecast/anomaly/variance/structure). Optional next: cut Donut, Lead-Insight merge, provenance dialog.
- [[โปรเจกต์ - ccgram Telegram|ccgram — Telegram as terminal]] — Control Claude/Codex/Gemini + full shell from Telegram, terminal mirrored as PNG. Installed+configured 2026-06-10. Hooks done; needs token/user-id/group-id filled in `~/.ccgram/.env` then `ccgram run`. YOLO=per-window 🇾.
- [[โปรเจกต์ - Second Brain|Second Brain (Obsidian vault)]] — `~/SecondBrain/` PARA vault · memory auto-mirror ทุก 30 นาที (launchd) · mcp-obsidian ใน Claude Code + Desktop · แก้ memory ที่ต้นทางเท่านั้น ห้ามแก้ `30 Claude Memory/`
- [[โปรเจกต์ - EasyCRM|EasyCRM (Loyalty/CRM product)]] — product ของบริษัทอีซี่สลิป · LINE OA + EasySlip · 5 packages ฿0-฿35,988/yr · portal `easycrm-two.vercel.app` · subdomain `<store>.easy-crm.co`. Knowledge base: `~/SecondBrain/EasyCRM/`
- [[โปรเจกต์ - BoostSMS|BoostSMS (SMS Marketing platform)]] — product ของบริษัทอีซี่สลิป · boost-sms.com / app.boost-sms.com / backoffice.boost-sms.com · 9 packages ฿0–฿1.2M · Source Code ฿2M ส่งมอบ ส.ค. 2568 · NBTC certified · Knowledge base: `~/SecondBrain/BoostSMS/`
- [[โปรเจกต์ - Friday Bot|Friday Bot — knowledge base]] — Friday AI bot "สมอง" อยู่ที่ `~/SecondBrain/Friday/` · Monthly Recaps + Themes + technical reference. เดือนแรก: 2026-05 (151 weekly + 426 daily summaries → 150k chars). Pattern: pull จาก D1 → group by chat → parallel agents per theme → assemble + HTML render.
- [[คู่มือ - SecondBrain Resources|SecondBrain Resources hub]] — `~/SecondBrain/03 Resources/` + brand subdirs. COO comms/Accounting SOP/AI Workshops/People/Tools + EasySlip docs + Thunder reports + BoostSMS materials. Imported 2026-06-12.
- [[คู่มือ - notebooklm-py|notebooklm-py CLI]] — Unofficial Python API for Google NotebookLM. `notebooklm` via `uv tool`. Login via Chrome cookies (rookiepy). Skill installed at `~/.claude/skills/notebooklm/`. 2026-06-13.
- [[Kbank API pricing + Easy×Thunder cross-billing|Kbank API pricing + Easy×Thunder cross-billing]] — Kbank tiered (0.04→0.025 KBank-source / flat 0.11 Others, ม.ค. 2569+). Thunder→Easy flat 0.04 K2K + 0.11 Others (no tier discount passed). Easy 95% via Thunder. Full doc in Obsidian Thunder Solution/Reports/.

## my-ai-bot (Friday AI)
- **Type:** Cloudflare Worker, single-file `src/index.js` (~3500 lines)
- **DB:** D1 SQLite, migrations in `migrations/` (currently 0001-0014)
- **Cron:** Every 3h → `proactiveAlert`, `proactiveInsightAlert`, `summarizeAndCleanup`
- **AI:** Gemini 2.5 Flash via direct API call (`askGemini` helper)
- **Telegram:** Bot API with callback queries (inline keyboards), `sendTelegramWithKeyboard`
- **Key tables:** messages, commitments, memories, summaries, file_cache, bot_messages, alerts, group_registry, allowed_users
- **Multi-user:** Role-based access (boss=full, member=limited, null=rejected). `getUserRole()`, `MEMBER_COMMANDS`, `MEMBER_CALLBACKS` sets. `/allow`, `/revoke`, `/users` commands. `handleMemberChat` = simplified AI (flash model, no actions)
- **Proactive Alert v2:** JSON output format, dedup via topic_fingerprint, urgency scoring (critical/high/medium/low), per-group gathering with priority_weight, real-time urgent alerts via regex, boss feedback learning
- **Callback format:** `pa:<mode>:<chatId>:<alertId>` where mode = s/d/h/x
- **Multi-instance:** Wrangler envs (`[env.daisy]`). `BOT_NAME` env var replaces hardcoded "Friday". `BOSS_NICKNAMES` env var (JSON array) for `detectBossMention`. Deploy: `deploy:all`, `db:migrate:all`
- **Syntax check:** `node -c src/index.js` (no test suite)
