---
title: Knowledge Graphs (graphify)
type: index
tags: [knowledge-graph, graphify, easyslip, thunder, boostsms]
created: 2026-07-17
---

# Knowledge Graphs — EasySlip · Thunder · BoostSMS

กราฟความรู้ที่สกัดจาก KB แต่ละแบรนด์ด้วย [[reference_graphify|graphify]] (any input → knowledge graph)
แต่ละโฟลเดอร์เป็น 1 กราฟ: note 1 ไฟล์ = 1 แนวคิด/เอนทิตี เชื่อมกันด้วย `[[wikilink]]` + มี `graph.canvas` เปิดดูผังกลุ่มได้

> [!tip] วิธีดูให้เห็นภาพรวม
> เปิด `graph.canvas` ในแต่ละโฟลเดอร์ (Obsidian Canvas) จะเห็น node จัดกลุ่มตาม community พร้อมสี
> หรือกด **Graph view** ของ Obsidian แล้ว filter `path:_KnowledgeGraphs/<แบรนด์>`

## กราฟในนี้

| โฟลเดอร์ | ที่มา | Nodes | Communities | God node เด่น |
|---|---|---|---|---|
| [[EasySlip/01 · EasySlip Overview\|EasySlip]] | `~/SecondBrain/EasySlip` (13 ไฟล์ 3 PDF) | 66 | 9 | สัญญา SCCN MOU |
| [[Thunder/Thunder Solution — Overview (KB doc)\|Thunder]] | `~/SecondBrain/Thunder Solution` (17 ไฟล์) | 84 | 11 | Thunder Solution Co./MOU KBank |
| BoostSMS | `~/SecondBrain/BoostSMS` (57 ไฟล์ dedup) | 237 | 18 | BoostSMS (แบรนด์) |
| **_Merged-CrossBrand** | รวม 3 แบรนด์ + เส้นข้ามแบรนด์ | 387 | 32 | BoostSMS + SCCN MOU |

## จุดที่กราฟรวมเจอ (cross-brand)

เชื่อม 12 เอนทิตีที่ทั้ง 3 แบรนด์ใช้ร่วมกัน (EasySlip, Thunder, KBank, EasyCRM, cross-billing, Master Dashboard ฯลฯ) จน 2 community กลายเป็นก้อนผสม 3 แบรนด์:
- **★ Cross-Brand: Companies & Dashboards** — EasyCRM/Master Dashboard โผล่ทั้ง 3 แบรนด์
- **★ Cross-Brand: API Pricing & Cost** — KBank cost + EasySlip API pricing + commission + MOU cost มารวมกลุ่ม

## หมายเหตุ (โครงสร้าง)

- โฟลเดอร์นี้ **นำเข้าจากกราฟต้นทาง** ที่ `~/Code/secondbrain-graphs/` (source of truth สำหรับ re-run)
- แก้/สร้างกราฟใหม่ → รันที่ workspace ต้นทางแล้ว copy `*.md` + `graph.canvas` เข้ามาใหม่ (อย่า copy `.obsidian/` เข้ามาเด็ดขาด — จะทับ setting vault)
- ไม่แก้ note ในนี้เอง (มันคือ output ของกราฟ ไม่ใช่ note ที่เขียนมือ)
