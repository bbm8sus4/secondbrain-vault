# 🧠 สมองที่สอง — หน้าหลัก

> สมองกลางของ AI Agent ทุกตัว (Claude Code / Claude Desktop / Codex)
> อัพเดตล่าสุด: 2026-06-21

## 🧭 Agent ต้องอ่านก่อน (Schema)

- **[[WIKI]]** ⭐ — กฎ wiki ของ vault นี้ (Karpathy LLM Wiki v2 — เวอร์ชั่นปรับปรุง)
- **[[index]]** — catalog ทุกหน้าสำคัญ จัดตาม PARA + Brand
- **[[log]]** — timeline append-only (`grep "^## \[" log.md | tail -10`)
- ก่อนเขียน wiki ใหม่ → อ่าน [[WIKI - Ingest Playbook]] · ก่อนตอบ → [[WIKI - Query Playbook]] · health check → [[WIKI - Lint Playbook]]

## 🔥 งานที่กำลังทำ (Active Work Index)

Agent อ่านตรงนี้ก่อนเสมอ

- [[โปรเจกต์ - Thunder Solution|Thunder Solution]] — slip verification + LINE chatbot · เป้าปี ฿125M
- [[โปรเจกต์ - EasyBOT Finance|EasyBOT Finance]] — AI product + finance webapp
- Friday AI — Telegram bot บน Cloudflare Worker (repo: `my-ai-bot`)
- [[โปรเจกต์ - ccgram Telegram|ccgram]] — คุม Claude/Codex จาก Telegram

## 🗺️ แผนที่ความรู้

- [[index]] ⭐ — vault catalog (ใหม่ 2026-06-21)
- [[ดัชนีความจำ]] — สารบัญ memory ทั้งหมดของ Claude (hub กลาง)
- [[แผนที่ - โปรเจกต์]] — โปรเจกต์ทั้งหมด
- [[แผนที่ - กติกา]] — กติกาการทำงานของ AI
- [[แผนที่ - คู่มือ]] — วิธีทำที่เคยเวิร์ก
- [[แผนที่ - Security]] — security + เส้นทางเรียน
- [[คู่มือ vault]] — วิธีดูแล vault นี้ (โครงสร้าง · ตั้งชื่อ · ระบบอัตโนมัติ · ห้ามแก้อะไร)
- [[กติกาการทำงาน]] — กติกา vault นี้

## 📁 โครงสร้าง Vault

| โฟลเดอร์ | ใช้ทำอะไร |
|---|---|
| `00 Inbox` | โน้ตใหม่ที่ยังไม่จัด — ทิ้งไว้ก่อน ค่อยย้าย |
| `01 Projects` | โปรเจกต์ที่มี deadline ชัด |
| `02 Areas` | งานต่อเนื่องไม่มีวันจบ (การเงิน, ทีม, สุขภาพ) |
| `03 Resources` | ความรู้อ้างอิง (playbook, แผนที่ความรู้) |
| `04 Archive` | จบแล้ว เก็บไว้ค้น |
| `10 Daily` | daily note |
| `20 Rules` | กติกาการทำงานของ AI — อ่านก่อนลงมือ |
| `30 Claude Memory` | mirror อัตโนมัติจาก Claude Code memory (read-only) |
| `40 Meeting Notes` | mirror จาก repo `meeting-notes` (สรุปประชุมอัตโนมัติ, read-only) |

## ⚙️ ระบบ

- Memory ต้นทาง: `~/.claude-warp/projects/-Users-aexgee/memory/` (ชื่อไฟล์อังกฤษ)
- Sync เข้า vault อัตโนมัติทุก 30 นาที + แปลงชื่อไฟล์เป็นไทย (`~/bin/sync-memory-to-vault.py`)
- Meeting notes: `git pull` + rsync เข้า `40 Meeting Notes/` ทุก 30 นาที (สคริปต์เดียวกัน)
- Telegram capture: ส่ง/forward ข้อความหาบอท → เข้า `00 Inbox/Telegram <วันที่>.md` ภายใน 1 นาที (`~/bin/telegram-vault-capture.py`)
- **Inbox auto-router**: Web Clipper drop ไฟล์ `tags: clippings` → ย้ายเข้า `03 Resources/Clippings/` ภายใน 3 นาที (`~/bin/inbox-auto-ingest.py` · ดู [[WIKI]] §3.5)
- Codex CLI: สั่ง "จำ/บันทึก" → เขียนลง `00 Inbox/` (ตั้งใน `~/.codex/AGENTS.md` แล้ว)
- AI เข้าถึงผ่าน mcp-obsidian → vault path `~/SecondBrain`
